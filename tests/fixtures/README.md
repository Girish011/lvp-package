# Test fixtures

Synthetic videos are generated by `tests/conftest.py` using FFmpeg lavfi
sources (SMPTE color bars + sine tone). They are license-clear and local.

Do not commit large copyrighted evaluation clips here — use `uploads/`
(gitignored) or the `benchmarks/download_benchmarks.py` helpers.
